<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Be My Valentine?</title>
    <style>
        :root {
            --primary-pink: #ff4d6d;
            --soft-pink: #fff0f3;
            --deep-red: #c9184a;
        }

        body, html {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            background-color: var(--soft-pink);
            font-family: 'Dancing Script', cursive, sans-serif;
        }

        /* --- Falling Hearts Background --- */
        .heart {
            position: absolute;
            color: var(--primary-pink);
            opacity: 0.6;
            pointer-events: none;
            animation: fall linear forwards;
        }

        @keyframes fall {
            to { transform: translateY(100vh) rotate(360deg); }
        }

        /* --- Professional Overlay --- */
        #overlay {
            position: fixed;
            inset: 0;
            background: linear-gradient(135deg, #ff8fa3 0%, #ffb3c1 100%);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 1000;
            transition: all 1.2s cubic-bezier(0.85, 0, 0.15, 1);
        }

        .photo-stack {
            display: flex;
            gap: 20px;
            margin: 40px 0;
            perspective: 1000px;
        }

        .photo-stack img {
            width: 180px;
            height: 240px;
            object-fit: cover;
            border: 8px solid white;
            border-radius: 4px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.15);
            transition: transform 0.5s ease;
        }

        .photo-stack img:hover {
            transform: scale(1.1) translateY(-20px) rotate(0deg) !important;
            z-index: 10;
        }

        .slide-up-btn {
            background: white;
            color: var(--primary-pink);
            padding: 18px 45px;
            border: none;
            border-radius: 50px;
            font-size: 1.5rem;
            font-weight: bold;
            cursor: pointer;
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
            transition: 0.3s;
        }

        .slide-up-btn:hover {
            transform: scale(1.05);
            background: var(--soft-pink);
        }

        /* --- Main UI --- */
        .main-content {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        h1 {
            color: var(--deep-red);
            font-size: 4rem;
            margin-bottom: 2rem;
            text-shadow: 2px 2px 10px rgba(0,0,0,0.05);
        }

        .btn-container {
            position: relative;
            width: 100%;
            height: 300px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        #yesBtn {
            background-color: var(--primary-pink);
            color: white;
            padding: 20px 50px;
            border: none;
            border-radius: 50px;
            font-size: 1.8rem;
            cursor: pointer;
            box-shadow: 0 8px 15px rgba(255, 77, 109, 0.3);
            transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            z-index: 5;
        }

        #noBtn {
            background-color: #f8ad9d;
            color: white;
            padding: 15px 35px;
            border: none;
            border-radius: 50px;
            font-size: 1.2rem;
            cursor: pointer;
            position: absolute;
            transition: 0.4s cubic-bezier(0.23, 1, 0.32, 1);
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&display=swap" rel="stylesheet">
</head>
<body>

    <div id="overlay">
        <h1 style="color: white;">I have a question for you...</h1>
        <div class="photo-stack">
            <img src="1001190429.jpg" style="transform: rotate(-10deg);">
            <img src="1001190430.jpg" style="transform: rotate(5deg);">
            <img src="1001190448.jpg" style="transform: rotate(-5deg);">
            <img src="1001190642.jpg" style="transform: rotate(10deg);">
        </div>
        <button class="slide-up-btn" onclick="startLove()">Click Me ❤️</button>
    </div>

    <div class="main-content">
        <h1>Will you be my Valentine?</h1>
        <div class="btn-container">
            <button id="yesBtn" onclick="celebrate()">YES!</button>
            <button id="noBtn" onmouseover="moveNo()">No</button>
        </div>
    </div>

    <script>
        // Create falling hearts background
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('heart');
            heart.innerHTML = '❤️';
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.animationDuration = Math.random() * 3 + 2 + 's';
            heart.style.fontSize = Math.random() * 20 + 10 + 'px';
            document.body.appendChild(heart);
            
            setTimeout(() => heart.remove(), 5000);
        }
        setInterval(createHeart, 300);

        function startLove() {
            document.getElementById('overlay').style.transform = 'translateY(-100%)';
        }

        let yesScale = 1;
        function moveNo() {
            const noBtn = document.getElementById('noBtn');
            const yesBtn = document.getElementById('yesBtn');
            
            // Random movement within a safe boundary
            const x = Math.random() * (window.innerWidth - 200);
            const y = Math.random() * (window.innerHeight - 100);
            
            noBtn.style.left = `${x}px`;
            noBtn.style.top = `${y}px`;

            // Professional growth for YES button
            yesScale += 0.25;
            yesBtn.style.transform = `scale(${yesScale})`;
        }

        function celebrate() {
            document.querySelector('.main-content').innerHTML = `
                <h1 style="font-size: 5rem; animation: pulse 1s infinite;">YAAAAY! ❤️</h1>
                <p style="font-size: 2rem; color: var(--deep-red);">You made me the happiest person ever!</p>
                <div style="font-size: 100px;">🥰</div>
            `;
            // Extra heart burst
            for(let i=0; i<50; i++) { setTimeout(createHeart, i*50); }
        }
    </script>
</body>
</html>